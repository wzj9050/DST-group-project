server:
  port: 8080

spring:
  thymeleaf:
    prefix: /templates #访问templates下的html模板
    cache: false #开发时关闭缓存，不然没办法看到实时页面
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 20MB
      #设置upload的上限
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/login?useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT
    username: root
    password: dd976789595
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update #新增数据库链接必备的参数
#mybatis:
#  mapper-locations: classpath*:mybatis/mapper/*.xml
